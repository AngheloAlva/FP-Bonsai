/* eslint-disable no-unused-vars */
import React from 'react'

const StartFreeButon = ({ padding, letterSpacing, fontWeight, minWidth, fontSize }) => {
  return (
    <button className='plan-card-btn' style={{ padding, letterSpacing, fontWeight, minWidth, fontSize }}>
      START FREE
    </button>
  )
}

export default StartFreeButon
